// TS Code :
var age = 25;
age = 30; // statically typed - types are checked at compile time
console.log;
// developers - can catch errors early in the development process
function add(num1, num2) {
    return num1 + num2;
}
var res = add(50, 90);
console.log(res);
// TS - better code autocomplete and intellisense support in code editors
var person = {
    name: "XYZ",
    age: 35
};
console.log(person.name);
